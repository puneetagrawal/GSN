require 'spec_helper'

	# before do
 #      create_user_identity
 #    end
	# describe "users/index.html.erb" do
	#   it "redirect for each event" do
	#   	create_user_identity
	#     assign(:users, [stub_model(User), stub_model(User)])
	#     assign(:users, [mock_model(User, :id => 1, :name => "First"),
 #      mock_model(User, :id => 2, :name => "Second")
 #    ])
	#     # assert_redirected_to users_path
 #        render
	#     expect(view).to render_template(:partial => "_user", :count => 2)
	#   end
	# end

# describe "events/show" do
#   it "displays the event location" do
#     assign(:event, stub_model(Event,
#       :location => "Chicago"
#     ))
#     render
#     expect(rendered).to include("Chicago")
#   end
# end