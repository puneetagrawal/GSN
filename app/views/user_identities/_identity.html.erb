<li>
  <%#= gravatar_for user, size: 52 %>
  <%= link_to identity.nickname, identity %>
  <%# if current_user.admin && !current_user?(identity) %>
    | <%#= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <%# end %>
</li>