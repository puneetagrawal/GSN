<script>

  graph_data = JSON.parse(('<%= raw [parsed_data.to_json][0].gsub("\\", "") %>'))
  console.log(graph_data)
  var node_ids=[]
  <% @check_node.each do |node| %> 
    node_ids.push(<%= node %>)
  <% end %>

  load_graph(graph_data, node_ids)

</script>